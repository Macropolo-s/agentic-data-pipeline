version: '3.8'

services:
  agentic-pipeline:
    build: .
    container_name: data_agent
    ports:
      - "8501:8501"
    volumes:
      - ./data_lake:/app/data_lake # Persist the 'S3' storage
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always
